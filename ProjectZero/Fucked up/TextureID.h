#ifndef TEXTURE_ID
#define TEXTURE_ID
namespace Textures {
	enum ID {
		Title1, Title2, Zero, X, Drone, Background, Platform,
		ButtonNormal, ButtonSelected, ButtonPressed,
		Bullet, Missile, Slash1, Slash2, Slash3,
		HealthRefill, MissileRefill, FireSpread, FireRate,
		Highway
	};
}
namespace Fonts {
	enum ID {
		Sansation, Main
	};
}
#endif 
#ifndef TEXTURE_ID
#define TEXTURE_ID
namespace Textures {
	enum ID {
		Title1, Title2, Zero, Background, Platform,
		ButtonNormal, ButtonSelected, ButtonPressed,
		Bullet, Missile, Attack,
		HealthRefill, MissileRefill, FireSpread, FireRate,
		Enemies,
		Highway,
		Animations
	};
}
namespace Fonts {
	enum ID {
		Sansation, Main
	};
}
#endif 